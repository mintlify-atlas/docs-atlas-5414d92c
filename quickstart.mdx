---
title: Quickstart
description: Get started with Griffo in vanilla JavaScript, React, or Motion
---

This guide will help you create your first text animation with Griffo. Choose the approach that matches your project.

## Choose Your Path

<Tabs>
  <Tab title="Vanilla JavaScript">
    <Steps>
      <Step title="Import splitText">
        Import the core `splitText` function from Griffo:
        
        ```ts
        import { splitText } from "griffo";
        import { animate, stagger } from "motion";
        ```
      </Step>

      <Step title="Wait for fonts to load">
        Wrap your code in `document.fonts.ready` to ensure accurate measurements:
        
        ```ts
        document.fonts.ready.then(() => {
          // Split and animate here
        });
        ```
        
        <Note>This ensures kerning measurements are accurate even with custom fonts</Note>
      </Step>

      <Step title="Split the text">
        Select an element and split it into characters, words, or lines:
        
        ```ts
        const element = document.querySelector("h1");
        const { chars, words, lines } = splitText(element, {
          type: "chars,words,lines"
        });
        ```
        
        <Tip>You can split by any combination: `"chars"`, `"words"`, `"lines"`, or `"chars,words,lines"`</Tip>
      </Step>

      <Step title="Animate the elements">
        Use your preferred animation library to animate the split elements:
        
        ```ts
        animate(
          chars,
          { opacity: [0, 1], y: [20, 0] },
          { delay: stagger(0.02) }
        );
        ```
      </Step>
    </Steps>

    ### Complete Example

    ```ts
    import { splitText } from "griffo";
    import { animate, stagger } from "motion";

    document.fonts.ready.then(() => {
      const { chars } = splitText(document.querySelector("h1"), {
        type: "chars"
      });
      
      animate(
        chars,
        { opacity: [0, 1], y: [20, 0] },
        { delay: stagger(0.02) }
      );
    });
    ```

    ### Advanced: Masking for Reveal Animations

    Use the `mask` option to create reveal animations where characters slide in from behind a clip container:

    ```ts
    document.fonts.ready.then(() => {
      const { words } = splitText(element, {
        type: "words",
        mask: "words" // wraps each word in overflow: clip container
      });
      
      animate(
        words,
        { y: ["100%", "0%"] },
        { delay: stagger(0.1) }
      );
    });
    ```

    ### Cleanup: Reverting

    Restore the original HTML when animations complete:

    ```ts
    const { chars, revert } = splitText(element, { type: "chars" });
    
    const animation = animate(chars, { opacity: [0, 1] });
    animation.finished.then(() => {
      revert(); // restores original HTML
    });
    ```

    Or use the automatic `revertOnComplete` option:

    ```ts
    splitText(element, {
      type: "chars",
      onSplit: ({ chars }) => animate(chars, { opacity: [0, 1] }),
      revertOnComplete: true // automatically reverts when animation finishes
    });
    ```
  </Tab>

  <Tab title="React">
    <Steps>
      <Step title="Import SplitText">
        Import the React component from `griffo/react`:
        
        ```tsx
        import { SplitText } from "griffo/react";
        import { animate, stagger } from "motion";
        ```
      </Step>

      <Step title="Wrap your text">
        Wrap any text element with `<SplitText>` and configure split options:
        
        ```tsx
        <SplitText options={{ type: "words" }}>
          <h1>Hello World</h1>
        </SplitText>
        ```
      </Step>

      <Step title="Add animation callback">
        Use the `onSplit` callback to animate elements when they're ready:
        
        ```tsx
        <SplitText
          options={{ type: "words" }}
          onSplit={({ words }) => {
            animate(
              words,
              { opacity: [0, 1], y: [20, 0] },
              { delay: stagger(0.05) }
            );
          }}
        >
          <h1>Hello World</h1>
        </SplitText>
        ```
      </Step>
    </Steps>

    ### Complete Example

    ```tsx
    import { SplitText } from "griffo/react";
    import { animate, stagger } from "motion";

    function Hero() {
      return (
        <SplitText
          options={{ type: "words" }}
          onSplit={({ words }) => {
            animate(
              words,
              { opacity: [0, 1], y: [20, 0] },
              { delay: stagger(0.05) }
            );
          }}
        >
          <h1>Hello World</h1>
        </SplitText>
      );
    }
    ```

    ### Advanced: Viewport Animations

    Animate when text scrolls into view using the `viewport` and `onViewportEnter` props:

    ```tsx
    <SplitText
      options={{ type: "words" }}
      initialStyles={{
        words: { opacity: 0, transform: "translateY(20px)" }
      }}
      viewport={{ amount: 0.5 }}
      onViewportEnter={({ words }) =>
        animate(words, { opacity: 1, y: 0 }, { delay: stagger(0.05) })
      }
      resetOnViewportLeave
    >
      <p>Animates when scrolled into view</p>
    </SplitText>
    ```

    ### Advanced: Responsive Re-splitting

    Automatically re-split and re-animate when the container resizes:

    ```tsx
    <SplitText
      options={{ type: "words", autoSplit: true }}
      onSplit={({ words }) => {
        animate(words, { opacity: [0, 1] }, { delay: stagger(0.05) });
      }}
      onResplit={({ words }) => {
        // Re-animate after resize
        animate(words, { opacity: [0, 1] }, { delay: stagger(0.05) });
      }}
    >
      <h1>Responsive Text</h1>
    </SplitText>
    ```

    <Note>The `waitForFonts` prop is `true` by default, so React components automatically wait for fonts before splitting</Note>
  </Tab>

  <Tab title="Motion (Declarative)">
    <Steps>
      <Step title="Import SplitText from Motion">
        Import the Motion-enhanced component:
        
        ```tsx
        import { SplitText } from "griffo/motion";
        import { stagger } from "motion";
        ```
      </Step>

      <Step title="Use Motion animation props">
        Use familiar Motion props like `initial`, `animate`, and `transition`:
        
        ```tsx
        <SplitText
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.65, delay: stagger(0.04) }}
          options={{ type: "words" }}
        >
          <h1>Hello World</h1>
        </SplitText>
        ```
      </Step>

      <Step title="Target specific split types">
        Apply different animations to chars, words, and lines:
        
        ```tsx
        <SplitText
          initial={{
            chars: { opacity: 0, y: 10 },
            words: { opacity: 0 }
          }}
          animate={{
            chars: { opacity: 1, y: 0 },
            words: { opacity: 1 }
          }}
          options={{ type: "chars,words" }}
        >
          <h1>Multi-level Animation</h1>
        </SplitText>
        ```
      </Step>
    </Steps>

    ### Complete Example

    ```tsx
    import { SplitText } from "griffo/motion";
    import { stagger } from "motion";

    function Hero() {
      return (
        <SplitText
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.65, delay: stagger(0.04) }}
          options={{ type: "words" }}
        >
          <h1>Hello World</h1>
        </SplitText>
      );
    }
    ```

    ### Advanced: Scroll Animations

    Use `whileInView` for scroll-triggered animations:

    ```tsx
    <SplitText
      initial={{ opacity: 0, y: 20 }}
      whileInView={{ opacity: 1, y: 0 }}
      transition={{ delay: stagger(0.05) }}
      viewport={{ once: true, amount: 0.5 }}
      options={{ type: "words" }}
    >
      <p>Animates when scrolled into view</p>
    </SplitText>
    ```

    ### Advanced: Variants with Function Callbacks

    Use function variants to access element metadata like `lineIndex`:

    ```tsx
    <SplitText
      variants={{
        hidden: { chars: { opacity: 0, y: 10 } },
        visible: {
          chars: ({ lineIndex }) => ({
            opacity: 1,
            y: 0,
            transition: {
              delay: stagger(0.02, { startDelay: lineIndex * 0.15 }),
            },
          }),
        },
      }}
      initial="hidden"
      animate="visible"
      options={{ type: "chars,lines", mask: "lines" }}
    >
      <p>Per-line staggered reveal</p>
    </SplitText>
    ```

    ### Advanced: Masking for Reveal Animations

    Combine `mask` with Motion animations for smooth reveals:

    ```tsx
    <SplitText
      initial={{ y: "100%" }}
      animate={{ y: "0%" }}
      transition={{ delay: stagger(0.1) }}
      options={{ type: "words", mask: "words" }}
    >
      <h1>Sliding Reveal</h1>
    </SplitText>
    ```
  </Tab>
</Tabs>

## Common Patterns

### Character-by-Character Fade In

<CodeGroup>
```ts Vanilla
const { chars } = splitText(element, { type: "chars" });
animate(chars, { opacity: [0, 1] }, { delay: stagger(0.02) });
```

```tsx Motion
<SplitText
  initial={{ opacity: 0 }}
  animate={{ opacity: 1 }}
  transition={{ delay: stagger(0.02) }}
  options={{ type: "chars" }}
>
  <h1>Text</h1>
</SplitText>
```
</CodeGroup>

### Word Slide Up

<CodeGroup>
```ts Vanilla
const { words } = splitText(element, { type: "words" });
animate(
  words,
  { opacity: [0, 1], y: [20, 0] },
  { delay: stagger(0.05) }
);
```

```tsx Motion
<SplitText
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ delay: stagger(0.05) }}
  options={{ type: "words" }}
>
  <p>Text</p>
</SplitText>
```
</CodeGroup>

### Line-by-Line Reveal with Mask

<CodeGroup>
```ts Vanilla
const { lines } = splitText(element, {
  type: "lines",
  mask: "lines"
});
animate(
  lines,
  { y: ["100%", "0%"] },
  { delay: stagger(0.15) }
);
```

```tsx Motion
<SplitText
  initial={{ y: "100%" }}
  animate={{ y: "0%" }}
  transition={{ delay: stagger(0.15) }}
  options={{ type: "lines", mask: "lines" }}
>
  <h2>Text</h2>
</SplitText>
```
</CodeGroup>

## Important Notes

<Note>
  **Ligatures are disabled:** Griffo automatically sets `font-variant-ligatures: none` when splitting by characters, because ligatures can't span multiple elements.
</Note>

<Note>
  **Accessibility is automatic:** Headings get `aria-label`, generic elements get a screen-reader-only copy. Your animations remain accessible to assistive technology.
</Note>

<Tip>
  **Use `initialStyles` for instant visibility:** Set initial opacity or transforms via the `initialStyles` option to prevent flash of unstyled content:
  
  ```tsx
  <SplitText
    options={{ type: "words" }}
    initialStyles={{
      words: { opacity: 0, transform: "translateY(10px)" }
    }}
    onSplit={({ words }) => animate(words, { opacity: 1, y: 0 })}
  >
    <h1>No Flash</h1>
  </SplitText>
  ```
</Tip>

## Next Steps

<CardGroup cols={2}>
  <Card title="Core API" icon="code" href="/api/core/split-text">
    Explore all options and return values for `splitText()`
  </Card>
  <Card title="React API" icon="react" href="/api/react/split-text">
    Learn about lifecycle props, viewport detection, and more
  </Card>
  <Card title="Motion API" icon="film" href="/api/motion/split-text">
    Dive into variants, custom values, and animation props
  </Card>
  <Card title="Vanilla Guide" icon="code" href="/guides/vanilla-js">
    Detailed guide for vanilla JavaScript usage
  </Card>
</CardGroup>